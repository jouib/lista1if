var teclado = require("prompt-sync")();
var numero_macas, teclado;
("Digite o n\u00FAmero de ma\u00E7as:");
var preco_total = 0;
if (numero_macas < 12)
    : preco_total = numero_macas * 0.30;
else
    : preco_total = numero_macas * 0.25;
